# Model candidate (benchmark required)

- Repository: `LiquidAI/LFM2.5-2.6B-GGUF`
- Revision: `not supplied`

## Rationale

The primary caretaker model (LFM2.5 2.6B Q4_K_M) is the main CPU inference target. The configuration shows it as the default with verified SHA256 hash and immutable revision. No immediate defect observed in the model itself.

## Evidence URLs

- https://github.com/adybag14-cyber/Githubinference/blob/main/config/models.json

This candidate cannot enter the automatic registry without an immutable revision, artifact digest, compatible license, CPU smoke/quality benchmarks, and a rollback plan.
